apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namePrefix: staging-

commonLabels:
  environment: staging

patchesStrategicMerge:
  - deployment-patch.yaml
  - configmap-patch.yaml

images:
  - name: ghcr.io/victorabaah/edge-app:v1
    # CI/CD updates this tag in GitOps promotion
    newTag: f22551cd64cc2af8b33c25d84a870ab95a407124
